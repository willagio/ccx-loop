---
id: T-1
title: "M9: external state directory — relocate .ccx/ to $XDG_DATA_HOME/ccx/<key>/"
scope:
  include:
    - plugins/ccx/commands/supervisor.md
    - plugins/ccx/commands/loop.md
    - plugins/ccx/commands/forever.md
    - plugins/ccx/commands/plan.md
    - docs/supervisor-design.md
  exclude: []
depends_on: []
---

# M9: external state directory — relocate .ccx/ to $XDG_DATA_HOME/ccx/<key>/

## Goal

Foundation for M9. Every other M9 task assumes state lives at
`$XDG_DATA_HOME/ccx/<repo-key>/` (Linux/macOS) with `$CCX_DATA_HOME`
override. `<repo-key>` = SHA-256 short hash of first remote URL +
basename suffix (e.g. `my-project-a3f9b2`); fallback to realpath
hash if no remote.

Touch points across the four command files: every read/write to
`.ccx/BOARD.md`, `.ccx/tasks/`, `.ccx/audit/`, `.ccx/workers/` must
resolve through a single helper that:

1. Reads `$CCX_DATA_HOME` (override).
2. Else `$XDG_DATA_HOME/ccx/<key>/` (Linux).
3. Else `~/.local/share/ccx/<key>/` (Linux fallback) or
   `~/Library/Application Support/ccx/<key>/` (macOS).
4. Creates the directory on first access (silent + one stderr line:
   `ccx state: <path>`).
5. If `ccx.dogfood = true` in `git config`, short-circuit to
   repo-local `.ccx/`. This repo may also carry a committed
   `.ccx-config` as dogfood metadata; customer repos must not receive
   a ccx-owned config file.

`docs/supervisor-design.md` gains a new `## 18. M9 — Customer-mode
invisibility` section (M7/M8 depth) documenting the state path
resolver, the repo-key derivation, the dogfood flag, and the
invariant set this enables.

## Acceptance

- [ ] State path helper exists and is the single resolver across all
      four `plugins/ccx/commands/*.md` files. No remaining direct
      `.ccx/` literal in code paths other than the dogfood
      short-circuit branch.
- [ ] `$CCX_DATA_HOME` env var override works.
- [ ] `<repo-key>` derivation is deterministic and stable across
      clones of the same remote URL.
- [ ] First-run creates the directory silently with a one-line
      stderr announcement.
- [ ] `ccx.dogfood = true` short-circuits to repo-local `.ccx/`.
- [ ] `docs/supervisor-design.md` §18 documents the resolver
      (depth comparable to §15/§16).
- [ ] Changes limited to paths matching `scope.include`.
- [ ] Codex review returns `verdict: "approve"` with zero in-scope
      findings at the worker's `--min-severity`.

## Context

Source: `BOARD.md`. Project direction at dispatch time:

> Active milestone: **M9 — Customer-mode invisibility (zero footprint in user repos)**. Motivation: ccx today leaves heavy traces in any repo it touches — `.ccx/` directory in the worktree, BOARD.md at repo root, `ccx/T-X` branches surviving into merge commits, `T-X:` / `supervisor: dispatch / supervisor: update board` commit subjects, and board/task file changes mixed into product history. That pattern is fine here (this repo is dogfood — the ccx narrative *is* the product) but it is unacceptable in a customer's repo, where the user expects their git log to be indistinguishable from one written by hand. M9 plugs every leak vector by relocating tool state outside the working tree and forcing commit/merge hygiene, with a verifier that gates merges if any leak is detected.

Six invariants are enforced by `ccx verify` (T-6); T-1 enables invariant 1 (no `.ccx/` in the user's working tree).

Scope globs (hard constraint — do NOT edit outside):
- include: [plugins/ccx/commands/supervisor.md, plugins/ccx/commands/loop.md, plugins/ccx/commands/forever.md, plugins/ccx/commands/plan.md, docs/supervisor-design.md]
- exclude: []

## Out of scope

- Worktree relocation (T-2).
- Commit message hygiene (T-3).
- Merge strategy (T-4).
- Inspection helpers `/ccx:where`, `/ccx:board`, `/ccx:tasks` and
  `ccx link` / `ccx unlink` (T-5).
- `ccx verify` script + README customer-mode section (T-6).
- Any file outside the scope globs above.
- Pushing to remote, opening PRs, or creating tags.
- Modifying `BOARD.md` or any `.ccx/tasks/*.md` — those are supervisor-owned.

## Test plan

- Fresh test repo (no `.ccx/`, no `ccx.dogfood`): run
  `/ccx:supervisor --dry-run --parallel 1` with a single-task seed
  BOARD passed via `$CCX_DATA_HOME=/tmp/ccx-test`. Confirm
  `/tmp/ccx-test/<key>/BOARD.md` is created and the test repo's
  working tree shows zero new files (`git status` clean).
- This repo (dogfood): set `ccx.dogfood = true` (the config
  mechanism this task introduces), run the same supervisor command,
  confirm `.ccx/` inside the repo is used unchanged.
- Run the existing repo test suite (if any) — no regressions.
- `/ccx:loop` Phase 1 enforces the test gate automatically.

## Decisions

- q: Where does the per-repo `ccx.dogfood` flag live — in plugin config, repo-local file, git config, or somewhere else?
  a: Customer mode reads `git config ccx.dogfood true` (and sibling keys like `ccx.merge.strategy`, `ccx.commit.trailer`, `ccx.paranoid`). A repo-root ccx-owned file would violate zero-footprint. This repo may commit `.ccx-config` only as dogfood metadata, and verifier/docs must call that out as a dogfood-only exception.
- q: `$XDG_DATA_HOME` vs `$XDG_CACHE_HOME` for the default state path?
  a: `$XDG_DATA_HOME`. Board state is data (recoverable matters), not cache (evictable).
- q: First-run UX — silent, prompt, or single-line announcement?
  a: Single stderr line: `ccx state: <path>`. No interactive prompt. Survives non-TTY contexts.
- q: `<repo-key>` derivation when a repo has multiple remotes (e.g., `origin` and `upstream` for a fork)?
  a: Use `origin` if present; else first remote in `git remote` order; else fall back to realpath hash. Document this in §18.
- q: What about repos with no `origin` at all (local-only)?
  a: Fall back to SHA-256 short hash of `realpath` of the repo root. Mark these in the readable suffix as `<basename>-local-<hash>` to distinguish from remote-keyed entries.
